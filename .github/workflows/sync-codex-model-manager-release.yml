name: Sync Codex Model Manager Release

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Optional exact Codex Model Manager release tag, for example v0.2.0
        required: false
        type: string
  schedule:
    - cron: '17 0 * * *'

permissions:
  contents: write

concurrency:
  group: opl-homebrew-tap-write
  cancel-in-progress: false

jobs:
  sync:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Sync Cask from Codex Model Manager release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -n "$RELEASE_TAG" ]; then
            node scripts/sync-codex-model-manager-cask.mjs --release-tag "$RELEASE_TAG"
          else
            node scripts/sync-codex-model-manager-cask.mjs
          fi

      - name: Validate Codex Model Manager projection
        env:
          HOMEBREW_GITHUB_API_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          node test/codex-model-manager-cask.test.mjs
          node test/downstream-boundary.test.mjs
          brew tap "$GITHUB_REPOSITORY" "$GITHUB_WORKSPACE"
          tap_root="$(brew --repository "$GITHUB_REPOSITORY")"
          cp "$GITHUB_WORKSPACE/Casks/opl-codex-model-manager.rb" "$tap_root/Casks/opl-codex-model-manager.rb"
          if brew help trust >/dev/null 2>&1; then
            brew trust "$GITHUB_REPOSITORY"
          fi
          brew style --cask "$GITHUB_REPOSITORY/opl-codex-model-manager"
          brew audit --cask --online "$GITHUB_REPOSITORY/opl-codex-model-manager"

      - name: Commit Codex Model Manager Cask
        id: commit
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- Casks/opl-codex-model-manager.rb; then
            echo "No Codex Model Manager Cask update."
            exit 0
          fi
          git diff --quiet -- . ':(exclude)Casks/opl-codex-model-manager.rb' || {
            echo "::error::Codex Model Manager sync may modify only Casks/opl-codex-model-manager.rb."
            exit 1
          }
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Casks/opl-codex-model-manager.rb
          git commit -m "Sync Codex Model Manager Cask from release"
          git push origin HEAD:main
          echo "tap_commit=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Verify remote main
        if: ${{ steps.commit.outputs.tap_commit != '' }}
        env:
          TAP_COMMIT: ${{ steps.commit.outputs.tap_commit }}
        run: test "$(git ls-remote origin refs/heads/main | awk '{print $1}')" = "$TAP_COMMIT"
